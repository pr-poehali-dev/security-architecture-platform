# Dev-контейнер с hot reload (Vite dev server)
FROM node:20-alpine

WORKDIR /app

RUN npm install -g bun

COPY package.json bun.lockb* ./
RUN bun install

COPY . .

EXPOSE 5173

CMD ["bun", "run", "dev"]
